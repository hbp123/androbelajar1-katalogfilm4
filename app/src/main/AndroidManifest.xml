<?xml version="1.0" encoding="utf-8"?><manifest xmlns:android="http://schemas.android.com/apk/res/android"    xmlns:tools="http://schemas.android.com/tools"    package="com.herlambang.katalogfilm4">    <uses-permission android:name="android.permission.INTERNET" />    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />    <uses-permission android:name="android.permission.VIBRATE" />    <uses-permission android:name="android.permission.WAKE_LOCK" />    <permission        android:name="com.herlambang.katalogfilm4.WRITE_DATABASE"        android:protectionLevel="normal" />    <permission        android:name="com.herlambang.katalogfilm4.READ_DATABASE"        android:protectionLevel="normal" />    <application        android:allowBackup="true"        android:icon="@mipmap/ic_launcher"        android:label="@string/app_name"        android:roundIcon="@mipmap/ic_launcher_round"        android:supportsRtl="true"        android:theme="@style/AppTheme">        <activity            android:name=".views.main.MainActivity"            android:label="@string/app_name">            <intent-filter>                <action android:name="android.intent.action.MAIN" />                <category android:name="android.intent.category.LAUNCHER" />            </intent-filter>        </activity>        <activity android:name=".views.detail.DetailActivity" />        <provider            android:name=".provider.MovieProvider"            android:authorities="com.herlambang.katalogfilm4"            android:exported="true"            android:readPermission="com.herlambang.katalogfilm4.READ_DATABASE"            android:writePermission="com.herlambang.katalogfilm4.WRITE_DATABASE" />    </application></manifest>